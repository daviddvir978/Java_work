import java.util.Random;

import javax.sound.sampled.SourceDataLine;

public class GenerateRandomNumbersAndStrings {
    public static void main(String[] args) {
         //to generate random number - approach 1: Random class 
    Random rand = new Random();
    int rand_int = rand.nextInt(100);
    System.out.println(rand_int);
    double rand_double = rand.nextInt();
    System.out.println(rand_double);
    //to generate random number = approach 2: Math




        //to generate random number = approach 3: lang API
        
    }
    
}
